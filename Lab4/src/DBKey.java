//Lab4
public class DBKey {
	 //constructor of the keys
	 public DBKey(){
		 
	 }
	 //asks if the key is the key for "this" object
	 boolean isEqualTo(DBKey to) { 
		 return this == to; 
	 }  
}
